services:
  angular:
    build:
      context: ./frontend/auth-client
      dockerfile: Dockerfile
    container_name: auth-angular
    ports:
      - "4200:4200"
    volumes:
      - ./frontend/auth-client:/app
      - /app/node_modules 
    stdin_open: true
    tty: true